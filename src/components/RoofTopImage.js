import React from 'react';
import { Text, View } from 'react-native';

export default class RoofTopImage extends React.Component {

  //fetch function

  render() {

    return (
      <View>
        <Text> Fetch the image from the API </Text>
      </View>
    );
  }
}
